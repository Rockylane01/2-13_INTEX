<!-- This page is only visible to admins -->

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit User · Ella Rises</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Montserrat:wght@300;400;500;600;700&display=swap"
    rel="stylesheet">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
        rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet" />
</head>

<body>

  <%- include('../../partials/header') %>

  <div class="container">

    <!-- PAGE HEADER -->
    <div class="page-header">
      <h1 class="page-title">Edit User</h1>
      <p class="page-subtitle">Update member information for Ella Rises records.</p>
    </div>

    <!-- FORM CARD -->
    <div class="form-card">

      <form action="/editUser/<%= member.memberid %>" method="POST">

        <!-- FIRST NAME -->
        <div class="mb-3">
          <label for="memberfirstname" class="form-label">First Name</label>
          <input
            type="text"
            class="form-control"
            id="memberfirstname"
            name="memberfirstname"
            value="<%= member.memberfirstname %>"
            required>
        </div>

        <!-- LAST NAME -->
        <div class="mb-3">
          <label for="memberlastname" class="form-label">Last Name</label>
          <input
            type="text"
            class="form-control"
            id="memberlastname"
            name="memberlastname"
            value="<%= member.memberlastname %>"
            required>
        </div>

        <!-- EMAIL -->
        <div class="mb-3">
          <label for="memberemail" class="form-label">Email</label>
          <input
            type="email"
            class="form-control"
            id="memberemail"
            name="memberemail"
            value="<%= member.memberemail %>"
            required>
        </div>

        <!-- PHONE -->
        <div class="mb-3">
          <label for="memberphone" class="form-label">Phone Number</label>
          <input
            type="text"
            class="form-control"
            id="memberphone"
            name="memberphone"
            value="<%= member.memberphone %>"
            minlength="10"
            maxlength="10"
            required>
        </div>

        <!-- CITY -->
        <div class="mb-3">
          <label for="membercity" class="form-label">City</label>
          <input
            type="text"
            class="form-control"
            id="membercity"
            name="membercity"
            value="<%= member.membercity %>"
            required>
        </div>

        <!-- STATE -->
        <div class="mb-3">
          <label for="memberstate" class="form-label">State</label>
          <input
            type="text"
            class="form-control"
            id="memberstate"
            name="memberstate"
            value="<%= member.memberstate %>"
            required>
        </div>

        <!-- ZIP -->
        <div class="mb-3">
          <label for="memberzip" class="form-label">Zipcode</label>
          <input
            type="text"
            class="form-control"
            id="memberzip"
            name="memberzip"
            value="<%= member.memberzip %>"
            required>
        </div>

        <!-- SCHOOL/EMPLOYER -->
        <div class="mb-3">
          <label for="memberschooloremployer" class="form-label">School or Employer</label>
          <input
            type="text"
            class="form-control"
            id="memberschooloremployer"
            name="memberschooloremployer"
            value="<%= member.memberschooloremployer %>"
            required>
        </div>

        <!-- FIELD OF INTEREST -->
        <div class="mb-3">
          <label for="memberfieldofinterest" class="form-label">Field of Interest</label>
          <select id="memberfieldofinterest" name="memberfieldofinterest" class="form-select" required>
            <option value="">-- Select one --</option>
            <option value="STEM" <%= member.memberfieldofinterest === "STEM" ? "selected" : "" %>>STEM</option>
            <option value="Arts" <%= member.memberfieldofinterest === "Arts" ? "selected" : "" %>>Arts</option>
            <option value="Both" <%= member.memberfieldofinterest === "Both" ? "selected" : "" %>>Both</option>
          </select>
        </div>

        <!-- BUTTONS -->
        <div class="d-flex justify-content-between mt-4">
          <a href="/users" class="btn btn-outline-primary btn-sm-pill">Cancel</a>

          <button type="submit" class="btn btn-primary btn-sm-pill">
            Save Changes
          </button>
        </div>

      </form>
    </div>

    <div class="footer">
      &copy; <%= new Date().getFullYear() %> Ella Rises · Members
    </div>

  </div>

</body>
</html>
