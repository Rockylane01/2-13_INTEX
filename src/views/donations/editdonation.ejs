<!-- This page is only visible to logged-in admins. -->
<!-- Also i dunno why an admin would need (or should be able) to edit a donation as opposed to just delete it, but the rubric said to do it. woot. -->

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Donation · Ella Rises</title>

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <!-- Bootstrap + Site styles -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">
</head>

<body>
  <%- include('../../partials/header') %>

  <div class="container">

    <!-- PAGE HEADER -->
    <div class="page-header mt-4">
      <h1 class="page-title">Edit Donation</h1>
      <p class="page-subtitle">Update details for this contribution.</p>
    </div>

    <!-- FORM CARD -->
    <div class="form-card">

      <form action="/editdonation/<%= donation.donationid %>" method="POST">

        <!-- Donor name -->
        <div class="mb-3">
          <label class="form-label">Donor Name</label>
          <input 
            type="text" 
            class="form-control" 
            name="donorname" 
            value="<%= donation.donorname %>" 
            required
          >
        </div>

        <!-- Amount -->
        <div class="mb-3">
          <label class="form-label">Donation Amount ($)</label>
          <input 
            type="number" 
            step="0.01" 
            class="form-control" 
            name="donationamount" 
            value="<%= donation.donationamount %>" 
            required
          >
        </div>

        <!-- Date -->
        <div class="mb-3">
          <label class="form-label">Donation Date</label>
          <input 
            type="date" 
            class="form-control" 
            name="donationdate" 
            value="<%= donation.donationdate ? new Date(donation.donationdate).toISOString().slice(0,10) : '' %>" 
            required
          >
        </div>

        <!-- Buttons -->
        <div class="d-flex" style="gap: 10px;">
          <button type="submit" class="btn btn-primary btn-sm-pill">Save Changes</button>
          <a href="/donations" class="btn btn-outline-primary btn-sm-pill">Cancel</a>
        </div>

      </form>
    </div>

    <div class="footer">
      &copy; <%= new Date().getFullYear() %> Ella Rises · Edit Donation
    </div>

  </div>

</body>
</html>
