<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %> · Ella Rises</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=Montserrat:wght@300;400;500;600;700&display=swap"
    rel="stylesheet">

  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
    rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet" />
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light ella-nav">
    <div class="container">
      <a class="navbar-brand d-flex align-items-center" href="/">
        <img src="/images/ella-logo.png" alt="Ella Rises Logo" class="ella-logo">
        <span>Ella Rises</span>
      </a>
      <div class="navbar-nav ms-auto ella-nav-links">
        <a class="nav-link" href="/events">Events</a>
        <a class="nav-link" href="/donations">Donations</a>
        <a class="nav-link" href="/login">Login</a>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="page-header">
      <h1 class="page-title">Participants</h1>
      <p class="page-subtitle">
        View and manage participants engaged in Ella Rises programs and milestones.
      </p>
    </div>

    <div class="data-card">
      <div class="search-row">
        <input
          type="text"
          class="form-control"
          placeholder="Search by name, school, or program">
        <button class="btn btn-outline-primary btn-sm-pill" type="button">
          Search
        </button>
        <!-- later: only show this button if user is a manager -->
        <button class="btn btn-primary btn-sm-pill ms-auto" type="button">
          Add participant
        </button>
      </div>

      <div class="table-responsive">
        <table class="table align-middle mb-0">
          <thead>
            <tr>
              <th>Name</th>
              <th>Program</th>
              <th>School</th>
              <th>Milestones</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% if (participants && participants.length > 0) { %>
              <% participants.forEach(p => { %>
                <tr>
                  <td><%= p.name %></td>
                  <td><%= p.program %></td>
                  <td><%= p.school %></td>
                  <td><%= p.milestonesSummary %></td>
                  <td>
                    <!-- later: wrap these in manager-only checks -->
                    <button class="btn btn-outline-primary btn-sm-pill">
                      View
                    </button>
                    <button class="btn btn-outline-primary btn-sm-pill">
                      Edit
                    </button>
                  </td>
                </tr>
              <% }) %>
            <% } else { %>
              <tr>
                <td colspan="5" class="text-center text-muted">
                  No participants to display yet.
                </td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>

    <div class="footer">
      &copy; <%= new Date().getFullYear() %> Ella Rises · Participants
    </div>
  </div>

  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js">
  </script>
</body>
</html>
