<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Survey Detail · Ella Rises</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">
</head>
<body>
  <%- include('../../partials/header') %>

  <div class="container mt-4">
    <h2>Survey for <%= survey.memberfirstname %> <%= survey.memberlastname %></h2>
    <p><strong>Event:</strong> <%= survey.eventname %></p>
    <p><strong>Submitted:</strong> <%= survey.surveysubmissiondate ? new Date(survey.surveysubmissiondate).toLocaleString() : '-' %></p>

    <table class="table table-bordered mt-3">
      <thead>
        <tr>
          <th>Aspect</th>
          <th>Score</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>Satisfaction</td>
          <td><%= survey.surveysatisfactionscore %></td>
        </tr>
        <tr>
          <td>Usefulness</td>
          <td><%= survey.surveyusefulnessscore %></td>
        </tr>
        <tr>
          <td>Instructor</td>
          <td><%= survey.surveyinstructorscore %></td>
        </tr>
        <tr>
          <td>Recommendation</td>
          <td><%= survey.surveyrecommendationscore %></td>
        </tr>
        <tr>
          <td><strong>Overall Score</strong></td>
          <td><strong><%= survey.surveyoverallscore %></strong></td>
        </tr>
        <tr>
          <td>Comments</td>
          <td><%= survey.surveycomments %></td>
        </tr>
      </tbody>
    </table>

    <a href="/surveys?eventid=<%= survey.eventid %>" class="btn btn-secondary mt-3">Back to Event Surveys</a>
  </div>
  <form method="POST" action="/deleteSurvey/<%= survey.peid %>?redirect=<%= encodeURIComponent('/registration/' + survey.eventid) %>"
        onsubmit="return confirm('Are you sure you want to delete this survey?');"
        class="mt-3">
    <button type="submit" class="btn btn-danger">Delete Survey</button>
  </form>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
